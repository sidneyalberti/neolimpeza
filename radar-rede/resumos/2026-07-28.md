# Radar de Rede — 2026-07-28

## Execução interrompida: variável `LINKEDIN_LI_AT` não configurada neste ambiente

A rotina verificou a variável de ambiente `LINKEDIN_LI_AT` (cookie de sessão do LinkedIn, necessário para autenticar o ator Apify que busca as conexões de 1º grau do Sidney) e ela **não está definida** neste ambiente de nuvem. Conforme instrução da rotina, a execução foi interrompida imediatamente neste ponto — nenhuma tentativa de busca no Apify, nenhuma leitura de posts/comentários e nenhuma checagem de histórico foi feita.

**Ação necessária do Sidney:** configurar a variável de ambiente `LINKEDIN_LI_AT` (valor do cookie de sessão `li_at` do LinkedIn) neste ambiente de nuvem para que a rotina consiga rodar. Sem isso, a rotina não tem como se autenticar no ator Apify e não pode continuar.

Nota: o histórico em `radar-rede/rodadas/` mostra que, nas rodadas anteriores (21 a 28/07), o problema era diferente — o cookie estava presente mas a conta Apify tinha excedido o limite mensal de uso (`Monthly usage hard limit exceeded`, 9 rodadas seguidas bloqueadas). Nesta execução, o próprio cookie não está presente no ambiente, o que é uma causa distinta e talvez indique que a variável precisa ser reconfigurada neste ambiente novo/atualizado.

## Itens por categoria
- Sinal comercial explícito: 0
- Comentário/menção sem resposta: 0
- Post novo relevante: 0

## Limitações desta rodada
- **`LINKEDIN_LI_AT` ausente**: nenhuma coleta foi possível. Nenhuma conexão, post ou comentário foi verificado.
