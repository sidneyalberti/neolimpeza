# Radar de Rede — 2026-07-29

## Execução interrompida: variável `LINKEDIN_LI_AT` não configurada nesta sessão

A rotina verificou a variável de ambiente `LINKEDIN_LI_AT` (cookie de sessão do LinkedIn, necessário para autenticar o ator Apify que busca as conexões de 1º grau do Sidney) e ela **não está definida** neste ambiente de nuvem. Conforme instrução da rotina, a execução foi interrompida imediatamente neste ponto — nenhuma tentativa de busca no Apify, nenhuma leitura de posts/comentários e nenhuma checagem de histórico foi feita.

**Ação necessária do Sidney:** configurar a variável de ambiente `LINKEDIN_LI_AT` (valor do cookie de sessão `li_at` do LinkedIn) neste ambiente de nuvem para que a rotina consiga rodar de forma autônoma, sem depender de fornecer o cookie manualmente a cada disparo.

Nota: existe registro de uma outra execução mais cedo hoje (`radar-rede/rodadas/2026-07-29.md`, fora do escopo desta rotina de resumos) em que o cookie `li_at` foi fornecido manualmente e a coleta foi bloqueada por limite de quota mensal da conta Apify (`Monthly usage hard limit exceeded`) — mesmo bloqueio reportado ininterruptamente desde 21/07. Nesta execução agendada, porém, o problema é anterior a isso: a variável de ambiente simplesmente não está presente na sessão, então nem chegou a haver tentativa de chamada ao ator Apify.

## Itens por categoria
- Sinal comercial explícito: 0
- Comentário/menção sem resposta: 0
- Post novo relevante: 0

## Limitações desta rodada
- **`LINKEDIN_LI_AT` ausente**: nenhuma coleta foi possível. Nenhuma conexão, post ou comentário foi verificado.
- Mesmo que a variável estivesse presente, há indícios (rodada anterior de hoje) de que a quota mensal da conta Apify segue esgotada — vale o Sidney confirmar o plano/créditos da conta Apify além de configurar a variável de ambiente.
