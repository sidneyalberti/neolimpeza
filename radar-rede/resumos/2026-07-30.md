# Radar de Rede — 2026-07-30 (execução agendada)

## Execução interrompida: variável `LINKEDIN_LI_AT` não configurada nesta sessão

A rotina verificou a variável de ambiente `LINKEDIN_LI_AT` (cookie de sessão do LinkedIn, necessário para autenticar o ator Apify que busca as conexões de 1º grau do Sidney) e ela **não está definida** neste ambiente de nuvem. Conforme instrução da rotina, a execução foi interrompida imediatamente neste ponto — nenhuma tentativa de busca no Apify, nenhuma leitura de posts/comentários e nenhuma checagem de histórico foi feita.

**Ação necessária do Sidney:** configurar a variável de ambiente `LINKEDIN_LI_AT` (valor do cookie de sessão `li_at` do LinkedIn) neste ambiente de nuvem para que a rotina agendada consiga rodar de forma autônoma.

Nota: existe também um registro de outra execução mais cedo hoje (`radar-rede/rodadas/2026-07-30.md`, fora do escopo desta rotina de resumos agendados) em que o cookie foi fornecido manualmente e a coleta foi bloqueada por limite de quota mensal da conta Apify (`Monthly usage hard limit exceeded`) — o mesmo bloqueio reportado ininterruptamente desde 21/07 (11ª rodada de coleta seguida bloqueada, cobrindo 10 dias corridos). Ou seja, mesmo que a variável de ambiente seja configurada nesta sessão agendada, é provável que a coleta continue bloqueada pela quota da conta Apify até isso ser resolvido separadamente.

## Itens por categoria
- Sinal comercial explícito: 0
- Comentário/menção sem resposta: 0
- Post novo relevante: 0

## Limitações desta rodada
- **`LINKEDIN_LI_AT` ausente** nesta sessão agendada: nenhuma coleta foi possível. Nenhuma conexão, post ou comentário foi verificado.
- **Bloqueio de quota Apify** (persistente desde 21/07, confirmado hoje mais cedo em rodada manual): mesmo com o cookie presente, é provável que a coleta continue bloqueada até o Sidney verificar plano/créditos da conta Apify.
- Dois problemas distintos e sobrepostos bloqueando a rotina há 10 dias corridos: (1) variável de ambiente ausente nas sessões agendadas, (2) quota mensal da Apify esgotada. Ambos precisam ser resolvidos para a rotina voltar a funcionar de ponta a ponta.
