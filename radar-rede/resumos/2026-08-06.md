# Radar de Rede — 2026-08-06 (execução agendada)

## Execução interrompida: variável `LINKEDIN_LI_AT` não configurada nesta sessão

A rotina verificou a variável de ambiente `LINKEDIN_LI_AT` (cookie de sessão do LinkedIn, necessário para autenticar o ator Apify que busca as conexões de 1º grau do Sidney) e ela **não está definida** neste ambiente de nuvem. Conforme instrução da rotina, a execução foi interrompida imediatamente neste ponto — nenhuma tentativa de busca no Apify, nenhuma leitura de posts/comentários e nenhuma checagem de histórico foi feita.

**Ação necessária do Sidney:** configurar a variável de ambiente `LINKEDIN_LI_AT` (valor do cookie de sessão `li_at` do LinkedIn) neste ambiente de nuvem para que esta rotina agendada consiga rodar de forma autônoma.

Esta é a **2ª execução desta rotina de resumos agendados hoje (06/08)** com o mesmo bloqueio — a variável já estava ausente na primeira checagem do dia e continua ausente agora. Somando com as rodadas anteriores (28/07, 29/07, 30/07 e 01/08), já são **6 execuções** desta rotina de resumos bloqueadas pelo mesmo motivo. Não há registro em `radar-rede/resumos/` entre 02/08 e 05/08, então não é possível confirmar se a rotina rodou nesses dias; de qualquer forma, a variável nunca foi configurada neste ambiente entre uma execução e outra.

Nota: existe também um registro separado de outra rotina (`radar-rede/rodadas/`, fora do escopo desta rotina de resumos agendados) em que o cookie foi fornecido manualmente em execuções anteriores e a coleta foi bloqueada por limite de quota mensal da conta Apify (`Monthly usage hard limit exceeded`) — bloqueio reportado ininterruptamente desde 21/07 até a última rodada registrada (06/08), 19 rodadas de coleta seguidas ao longo de 17 dias corridos. Ou seja, mesmo que a variável de ambiente seja configurada nesta sessão agendada, é provável que a coleta continue bloqueada pela quota da conta Apify até isso ser resolvido separadamente.

## Itens por categoria
- Sinal comercial explícito: 0
- Comentário/menção sem resposta: 0
- Post novo relevante: 0

## Limitações desta rodada
- **`LINKEDIN_LI_AT` ausente** nesta sessão agendada: nenhuma coleta foi possível. Nenhuma conexão, post ou comentário foi verificado.
- **Bloqueio de quota Apify** (persistente desde 21/07, confirmado pela última vez em 06/08 na rotina irmã `radar-rede/rodadas/`, 19ª rodada de coleta seguida, 17 dias corridos): mesmo com o cookie presente, é provável que a coleta continue bloqueada até o Sidney verificar plano/créditos da conta Apify.
- Dois problemas distintos e sobrepostos bloqueando a rotina: (1) variável de ambiente ausente nesta sessão agendada, (2) quota mensal da Apify esgotada. Ambos precisam ser resolvidos para a rotina voltar a funcionar de ponta a ponta.
