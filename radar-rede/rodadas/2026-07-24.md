# Radar de Rede — 2026-07-24

## Execução interrompida: variável `LINKEDIN_LI_AT` não configurada neste ambiente

A coleta desta rodada não pôde nem começar. Ao verificar a variável de ambiente `LINKEDIN_LI_AT` (cookie de sessão do LinkedIn necessário para autenticar o ator Apify de conexões), ela não está definida neste ambiente de nuvem. Conforme instrução da rotina, a execução foi interrompida imediatamente neste ponto — nenhuma chamada ao ator Apify foi feita, nenhum dado foi coletado.

Isso é diferente do bloqueio das três rodadas anteriores (2026-07-21, 22 e 23), que foram interrompidas por limite mensal de uso excedido na conta Apify, já com o cookie presumivelmente configurado. Hoje o bloqueio é anterior a isso: a variável de sessão do LinkedIn simplesmente não existe neste container.

**Nenhum dado foi coletado nesta rodada** — nenhuma conexão, nenhum post, nenhum comentário.

## Itens por categoria
- Sinal comercial explícito: 0
- Comentário/menção sem resposta: 0
- Post novo relevante: 0

## Limitações desta rodada
- **`LINKEDIN_LI_AT` ausente neste ambiente**: o Sidney precisa configurar essa variável de ambiente (cookie de sessão `li_at` do LinkedIn) nas configurações deste ambiente de nuvem antes que a rotina consiga rodar. Sem isso, nenhuma rodada futura vai conseguir coletar dados.
- Também vale confirmar, quando o cookie for configurado, se o limite mensal de uso da conta Apify (que bloqueou as rodadas de 21, 22 e 23/07) já foi renovado — do contrário a próxima rodada pode ser bloqueada por esse motivo mesmo depois de resolver o cookie.
