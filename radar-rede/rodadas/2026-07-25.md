# Radar de Rede — 2026-07-25

## Segunda tentativa do dia: execução interrompida — `LINKEDIN_LI_AT` não está mais configurado

Nesta segunda rodada de hoje, a variável de ambiente `LINKEDIN_LI_AT` (cookie de sessão do LinkedIn) **não está definida neste ambiente de nuvem**. Isso é uma regressão em relação à primeira tentativa de hoje (registrada abaixo), na qual o cookie estava disponível e chegou a ser usado como input — a coleta só não avançou por causa do bloqueio de quota da Apify. Conforme instrução da rotina, a execução foi interrompida imediatamente neste ponto, sem tentar contornar a ausência da variável.

**Nenhuma chamada à Apify foi feita nesta segunda tentativa.** É necessário que o Sidney reconfigure a variável `LINKEDIN_LI_AT` neste ambiente de nuvem (o valor pode ter se perdido entre sessões/containers) antes da próxima execução. Também vale revisitar, em paralelo, o bloqueio de quota mensal da Apify já registrado abaixo e nas rodadas de 21 a 25/07, pois mesmo com o cookie restaurado esse limite segue impedindo a coleta.

---

## Primeira tentativa do dia

## Execução interrompida: limite de uso mensal da Apify excedido (persistente, 6ª rodada de coleta bloqueada)

O cookie de sessão `li_at` do LinkedIn fornecido pelo Sidney seguia disponível para esta rodada. Com ele em mãos, a rotina tentou:

1. `scrapeflow/linkedin-network-scraper` (ator de conexões, autenticado com o `li_at` do Sidney) → **`Monthly usage hard limit exceeded`**
2. `harvestapi/linkedin-profile-posts` (ator de posts de perfil, sem autenticação) → **`Monthly usage hard limit exceeded`**

O mesmo bloqueio já havia sido registrado nas rodadas de 2026-07-21, 22, 23 e 24 (e também nas rodadas diárias da rotina de prospecção nesses mesmos dias, incluindo 24 e 25/07). Como o erro se repete em dois atores diferentes — um autenticado, um não —, confirma-se novamente que é a **conta Apify inteira** que está sem créditos/quota mensal, não um problema específico de ator ou do cookie do LinkedIn. Conforme instrução da rotina, a coleta foi interrompida imediatamente nesse ponto, sem tentar contornar o bloqueio.

**Nenhum dado foi coletado nesta rodada** — nenhuma conexão, nenhum post, nenhum comentário. Nenhuma checagem no HubSpot foi feita (não havia sinal comercial nenhum para checar).

O cookie `li_at` fornecido pelo Sidney não pôde ser validado como funcional ou expirado, porque o bloqueio de quota ocorreu antes de a Apify processar a autenticação da sessão do LinkedIn — mesma situação das rodadas anteriores.

## Itens por categoria
- Sinal comercial explícito: 0
- Comentário/menção sem resposta: 0
- Post novo relevante: 0

## Limitações desta rodada
- **Bloqueio de quota Apify (persistente há pelo menos 6 rodadas de coleta — 21, 22, 23, 24 e agora 25/07)**: limite de uso mensal da conta excedido. Já são 5 dias seguidos com o mesmo bloqueio mesmo após o cookie `li_at` ter sido fornecido — é necessário que o Sidney verifique o plano/créditos da conta Apify (aguardar renovação do ciclo mensal ou fazer upgrade), já que a situação não se resolveu sozinha. Esse mesmo bloqueio afeta também a rotina de prospecção social (`prospeccao-social`), o que reforça que é um limite de conta, não de rotina específica.
- Cookie `li_at` do LinkedIn: ainda disponível e usado como input desta rodada, mas sua validade segue não confirmada (bloqueio de quota ocorre antes da chamada autenticada ser processada). Recomenda-se reter o cookie mais recente para a próxima tentativa, já que ele pode expirar em algumas semanas e essa checagem segue pendente há vários dias.
- Sem coleta, não foi possível determinar a lista de conexões, posts novos ou comentários pendentes de resposta nos posts do Sidney.

## Histórico do dia
- Única tentativa: interrompida por limite de quota mensal da Apify excedido, em dois atores testados (mesmo padrão dos últimos 5 dias).
