# Radar de Rede — 2026-07-26

## Segunda tentativa do dia: execução interrompida — `LINKEDIN_LI_AT` não está configurado

Nesta segunda rodada de hoje, a variável de ambiente `LINKEDIN_LI_AT` (cookie de sessão do LinkedIn) **não está definida neste ambiente de nuvem**. Isso é uma regressão em relação à primeira tentativa de hoje (registrada abaixo), na qual o cookie estava disponível e chegou a ser usado como input — a coleta só não avançou por causa do bloqueio de quota da Apify, ainda ativo. Conforme instrução da rotina, a execução foi interrompida imediatamente neste ponto, sem tentar contornar a ausência da variável.

**Nenhuma chamada à Apify foi feita nesta segunda tentativa.** É a segunda vez em dois dias (25/07 e agora 26/07) que o cookie some entre uma rodada e outra do mesmo dia — o valor não está persistindo neste ambiente de nuvem entre sessões/containers. É necessário que o Sidney reconfigure a variável `LINKEDIN_LI_AT` de forma mais durável antes da próxima execução. O bloqueio de quota mensal da Apify (registrado na primeira tentativa abaixo e nas rodadas de 21 a 26/07) segue sem solução e continuaria impedindo a coleta mesmo com o cookie restaurado.

---

## Primeira tentativa do dia

## Execução interrompida: limite de uso mensal da Apify excedido (persistente, 7ª rodada de coleta bloqueada)

O cookie de sessão `li_at` do LinkedIn foi fornecido pelo Sidney nesta rodada (via prompt da rotina). Com ele em mãos, a rotina tentou:

1. `scrapeflow/linkedin-network-scraper` (ator de conexões, autenticado com o `li_at` do Sidney) → **`Monthly usage hard limit exceeded`**
2. `harvestapi/linkedin-profile-posts` (ator de posts de perfil, sem autenticação) → **`Monthly usage hard limit exceeded`**

O mesmo bloqueio já havia sido registrado ininterruptamente nas rodadas de 2026-07-21, 22, 23, 24 e 25 (duas tentativas nesse dia). Este é o 7º teste de coleta seguido bloqueado pelo mesmo erro, em dois atores diferentes — um autenticado, um não —, confirmando novamente que é a **conta Apify inteira** que está sem créditos/quota mensal, não um problema específico de ator ou do cookie do LinkedIn. Conforme instrução da rotina, a coleta foi interrompida imediatamente nesse ponto, sem tentar contornar o bloqueio.

**Nenhum dado foi coletado nesta rodada** — nenhuma conexão, nenhum post, nenhum comentário. Nenhuma checagem no HubSpot foi feita (não havia sinal comercial nenhum para checar).

O cookie `li_at` fornecido pelo Sidney não pôde ser validado como funcional ou expirado, porque o bloqueio de quota ocorre antes de a Apify processar a autenticação da sessão do LinkedIn — mesma situação das rodadas anteriores.

## Itens por categoria
- Sinal comercial explícito: 0
- Comentário/menção sem resposta: 0
- Post novo relevante: 0

## Limitações desta rodada
- **`LINKEDIN_LI_AT` ausente na segunda tentativa de hoje**: o cookie não persistiu entre as duas rodadas do dia — segunda ocorrência desse padrão em dois dias seguidos (25 e 26/07). Necessário reconfigurar de forma mais durável.
- **Bloqueio de quota Apify (persistente há 7 rodadas de coleta seguidas — 21, 22, 23, 24, 25 (2x) e 26/07 primeira tentativa)**: limite de uso mensal da conta excedido. Uma semana corrida com o mesmo bloqueio mesmo com o cookie `li_at` fornecido a cada rodada — é necessário que o Sidney verifique o plano/créditos da conta Apify (aguardar renovação do ciclo mensal ou fazer upgrade), já que a situação não se resolveu sozinha em nenhum dos dias anteriores.
- Cookie `li_at` do LinkedIn: fornecido na primeira tentativa de hoje, mas sua validade segue não confirmada (bloqueio de quota ocorre antes da chamada autenticada ser processada); ausente na segunda tentativa.
- Sem coleta em nenhuma das duas tentativas, não foi possível determinar a lista de conexões, posts novos ou comentários pendentes de resposta nos posts do Sidney.

## Histórico do dia
- Primeira tentativa: interrompida por limite de quota mensal da Apify excedido, em dois atores testados (mesmo padrão dos últimos 6 dias).
- Segunda tentativa: interrompida antes de qualquer chamada à Apify porque `LINKEDIN_LI_AT` não estava definido no ambiente.
