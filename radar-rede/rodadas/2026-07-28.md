# Radar de Rede — 2026-07-28

## Execução interrompida: limite de uso mensal da Apify excedido (persistente, 9ª rodada de coleta seguida bloqueada)

O cookie de sessão `li_at` do LinkedIn foi fornecido pelo Sidney nesta rodada (via prompt da rotina) e estava presente normalmente. Com ele em mãos, a rotina tentou:

1. `scrapeflow/linkedin-network-scraper` (ator de conexões de 1º grau, autenticado com o `li_at` do Sidney) → **`Monthly usage hard limit exceeded`**
2. `harvestapi/linkedin-profile-posts` (ator de posts de perfil, sem autenticação) → **`Monthly usage hard limit exceeded`**

O mesmo bloqueio já havia sido registrado ininterruptamente nas rodadas de 2026-07-21, 22, 23, 24, 25 (duas tentativas), 26 (duas tentativas), 27 e agora 28/07. Esta é a **9ª rodada de coleta seguida** bloqueada pelo mesmo erro, novamente confirmado em dois atores diferentes — um autenticado, um não —, reforçando que é a **conta Apify inteira** que está sem créditos/quota mensal, não um problema específico de ator ou do cookie do LinkedIn. Conforme instrução da rotina, a coleta foi interrompida imediatamente nesse ponto, sem tentar contornar o bloqueio.

A rotina irmã de prospecção (`prospeccao-social`) registrou o mesmo bloqueio hoje (commit `cf6f958`, "Apify bloqueado 9º dia seguido"), confirmando que o limite afeta a conta Apify como um todo, não apenas esta rotina.

**Nenhum dado foi coletado nesta rodada** — nenhuma conexão, nenhum post, nenhum comentário. Nenhuma checagem no HubSpot foi feita (não havia sinal comercial nenhum para checar).

O cookie `li_at` fornecido pelo Sidney nesta rodada não pôde ser validado como funcional ou expirado, porque o bloqueio de quota ocorre antes de a Apify processar a autenticação da sessão do LinkedIn — mesma situação das rodadas anteriores.

## Itens por categoria
- Sinal comercial explícito: 0
- Comentário/menção sem resposta: 0
- Post novo relevante: 0

## Limitações desta rodada
- **Bloqueio de quota Apify (persistente há 9 rodadas de coleta seguidas — 21, 22, 23, 24, 25 (2x), 26 (2x, sendo 1 sem tentativa Apify por falta de cookie), 27 e 28/07)**: limite de uso mensal da conta excedido. Mais de uma semana e meia corrida com o mesmo bloqueio mesmo com o cookie `li_at` fornecido a cada rodada em que houve tentativa — é necessário que o Sidney verifique o plano/créditos da conta Apify (aguardar renovação do ciclo mensal ou fazer upgrade), já que a situação não se resolveu sozinha em nenhum dos dias anteriores.
- Sem coleta nesta rodada, não foi possível determinar a lista de conexões, posts novos ou comentários pendentes de resposta nos posts do Sidney.

## Histórico do dia
- Tentativa única: interrompida por limite de quota mensal da Apify excedido, em dois atores testados (mesmo padrão dos últimos 8 dias).
