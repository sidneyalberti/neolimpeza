# Radar de Rede — 2026-08-01

## Execução interrompida: limite de uso mensal da Apify excedido (persistente, 13ª rodada de coleta seguida bloqueada)

O cookie de sessão `li_at` do LinkedIn foi fornecido pelo Sidney nesta rodada (via prompt da rotina) e estava presente normalmente. Com ele em mãos, a rotina tentou:

1. `scrapeflow/linkedin-network-scraper` (ator de conexões de 1º grau, autenticado com o `li_at` do Sidney) → **`Monthly usage hard limit exceeded`**
2. `harvestapi/linkedin-profile-posts` (ator de posts de perfil, não depende do cookie do Sidney, teste com URL pública de exemplo) → **`Monthly usage hard limit exceeded`**

O mesmo bloqueio já havia sido registrado ininterruptamente nas rodadas de 2026-07-21, 22, 23, 24, 25 (duas tentativas), 26 (duas tentativas), 27, 28 (duas tentativas), 29, 30 e 31/07. Esta é a **13ª rodada de coleta seguida** bloqueada pelo mesmo erro, agora cobrindo **12 dias corridos** sem nenhuma renovação de quota. A rodada de hoje testou novamente os mesmos dois atores — um autenticado com o cookie do Sidney, outro sem depender dele — e ambos retornaram o mesmo erro, confirmando mais uma vez que o diagnóstico de fundo continua o mesmo: **a conta Apify inteira está sem créditos/quota mensal**, não um problema específico de ator ou do cookie do LinkedIn.

**Nenhum dado foi coletado nesta rodada** — nenhuma conexão, nenhum post, nenhum comentário. Nenhuma checagem no HubSpot foi feita (não havia sinal comercial nenhum para checar).

O cookie `li_at` fornecido pelo Sidney nesta rodada não pôde ser validado como funcional ou expirado, porque o bloqueio de quota ocorre antes de a Apify processar a autenticação da sessão do LinkedIn — mesma situação das rodadas anteriores.

## Itens por categoria
- Sinal comercial explícito: 0
- Comentário/menção sem resposta: 0
- Post novo relevante: 0

## Limitações desta rodada
- **Bloqueio de quota Apify (persistente há 13 rodadas de coleta seguidas — 21, 22, 23, 24, 25 (2x), 26 (2x), 27, 28 (2x), 29, 30, 31/07 e 01/08, cobrindo 12 dias corridos)**: limite de uso mensal da conta excedido. O cookie `li_at` foi fornecido a cada rodada em que houve tentativa e a quota não se restabeleceu em nenhum dos dias anteriores nem nesta rodada. É necessário que o Sidney verifique o plano/créditos da conta Apify (aguardar renovação do ciclo de faturamento ou fazer upgrade de plano) — o ciclo mensal claramente não está resolvendo a situação sozinho e já passou de quase duas semanas de bloqueio contínuo.
- Sem coleta nesta rodada, não foi possível determinar a lista de conexões, posts novos ou comentários pendentes de resposta nos posts do Sidney.

## Histórico do dia
- Tentativa 1: `scrapeflow/linkedin-network-scraper` (conexões de 1º grau, autenticado) → interrompida por limite de quota mensal da Apify excedido.
- Tentativa 2: `harvestapi/linkedin-profile-posts` (posts de perfil, sem depender do cookie) → interrompida pelo mesmo limite de quota mensal da Apify excedido.
