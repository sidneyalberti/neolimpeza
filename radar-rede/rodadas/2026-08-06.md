# Radar de Rede — 2026-08-06

## Execução interrompida: limite de uso mensal da Apify excedido (persistente, 19ª rodada de coleta seguida bloqueada)

O cookie de sessão `li_at` do LinkedIn foi fornecido pelo Sidney nesta rodada (via prompt da rotina) e estava presente normalmente. Com ele em mãos, a rotina tentou novamente os dois mesmos atores testados em todas as rodadas anteriores:

1. `scrapeflow/linkedin-network-scraper` (ator de conexões de 1º grau, autenticado com o `li_at` do Sidney) → **`Monthly usage hard limit exceeded`**
2. `harvestapi/linkedin-profile-posts` (ator de posts de perfil, não depende do cookie do Sidney, teste com URL pública de exemplo) → **`Monthly usage hard limit exceeded`**

O mesmo bloqueio já havia sido registrado ininterruptamente nas rodadas de 2026-07-21 até 2026-08-05 (18 rodadas de coleta seguidas). Esta é a **19ª rodada de coleta seguida** bloqueada pelo mesmo erro, agora cobrindo **17 dias corridos** sem nenhuma renovação de quota visível — a virada do ciclo de faturamento em 01/08 não resolveu o bloqueio, confirmando que não se trata de um limite mensal que se renova automaticamente no calendário, e sim de uma quota/plano da conta que precisa de ação manual do Sidney (upgrade de plano ou compra de créditos adicionais). A rodada de hoje testou novamente os mesmos dois atores — um autenticado com o cookie do Sidney, outro sem depender dele — e ambos retornaram o mesmo erro, confirmando mais uma vez que o diagnóstico de fundo continua o mesmo: **a conta Apify inteira está sem créditos/quota**, não um problema específico de ator ou do cookie do LinkedIn.

**Nenhum dado foi coletado nesta rodada** — nenhuma conexão, nenhum post, nenhum comentário. Nenhuma checagem no HubSpot foi feita (não havia sinal comercial nenhum para checar).

O cookie `li_at` fornecido pelo Sidney nesta rodada não pôde ser validado como funcional ou expirado, porque o bloqueio de quota ocorre antes de a Apify processar a autenticação da sessão do LinkedIn — mesma situação das rodadas anteriores.

## Itens por categoria
- Sinal comercial explícito: 0
- Comentário/menção sem resposta: 0
- Post novo relevante: 0

## Limitações desta rodada
- **Bloqueio de quota Apify (persistente há 19 rodadas de coleta seguidas — 21, 22, 23, 24, 25 (2x), 26 (2x), 27, 28 (2x), 29, 30, 31/07, 01, 02, 03, 04, 05 e 06/08, cobrindo 17 dias corridos)**: limite de uso da conta excedido, e a virada de mês em 01/08 não renovou a quota — sinal de que não é um limite mensal que se resolve sozinho por calendário. É necessário que o Sidney verifique diretamente o plano/créditos da conta Apify (console.apify.com → Billing/Usage) e faça upgrade de plano ou compra de créditos — já passou de duas semanas e meia de bloqueio contínuo.
- Sem coleta nesta rodada, não foi possível determinar a lista de conexões, posts novos ou comentários pendentes de resposta nos posts do Sidney.

## Histórico do dia
- Tentativa 1: `scrapeflow/linkedin-network-scraper` (conexões de 1º grau, autenticado) → interrompida por limite de quota mensal da Apify excedido.
- Tentativa 2: `harvestapi/linkedin-profile-posts` (posts de perfil, sem depender do cookie) → interrompida pelo mesmo limite de quota mensal da Apify excedido.
