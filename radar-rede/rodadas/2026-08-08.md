# Radar de Rede — 2026-08-08

## Execução interrompida: cookie `li_at` inválido/expirado

O cookie de sessão `li_at` fornecido para esta rotina **falhou na autenticação com o LinkedIn**. Conforme a regra da rotina, a coleta foi interrompida imediatamente neste ponto — nenhuma conexão, post ou comentário foi coletado ou processado nesta rodada.

### Evidência do erro
- Ator `scrapeflow/linkedin-network-scraper` (conexões de 1º grau, autenticado): rodou com sucesso técnico (exit code 0) mas retornou **0 conexões**, tanto com enriquecimento de perfil ligado (57s de execução) quanto desligado, limitado a 5 conexões (92s de execução) — sem nenhum erro explícito no resultado do run.
- Como esse resultado (0 itens, sem erro visível) era ambíguo, foi feito um teste de diagnóstico com um segundo ator independente, `crawlerbros/linkedin-people-search-scraper`, usando o mesmo valor de cookie em uma busca simples por "Sidney Alberti". Este ator retornou a mensagem explícita: **"Could not establish LinkedIn session. Please provide fresh LinkedIn cookies."**
- Essa mensagem confirma que a causa raiz do resultado vazio no primeiro ator é o cookie `li_at` expirado ou inválido — não um problema pontual de um único ator.

### Ação necessária do Sidney
Capturar um novo valor de cookie `li_at` (login no linkedin.com → DevTools → Application → Cookies → `li_at`) e atualizar esta rotina com o novo valor. O cookie fornecido nesta execução não autenticou em nenhum dos dois atores testados.

## Itens por categoria
- Sinal comercial explícito: 0
- Comentário/menção sem resposta: 0
- Post novo relevante: 0

## Checagem HubSpot
Não realizada — nenhuma coleta de conexões ou posts foi possível nesta rodada, então não havia nenhum perfil com sinal comercial explícito para checar.

## Limitações desta rodada
- **Cookie `li_at` inválido/expirado**: bloqueou toda a coleta (conexões de 1º grau, posts das conexões, posts e comentários do próprio Sidney). Confirmado por mensagem de erro explícita de um ator de diagnóstico (`crawlerbros/linkedin-people-search-scraper`).
- Nenhum perfil privado, rate limit ou problema de quota Apify foi identificado — o bloqueio é especificamente de autenticação de sessão do LinkedIn.

## Histórico do dia
1. `scrapeflow/linkedin-network-scraper` (conexões de 1º grau, `enrichProfiles: true`, sem limite) → sucesso técnico, mas 0 itens retornados.
2. `scrapeflow/linkedin-network-scraper` (mesmo ator, `enrichProfiles: false`, `max_connections: 5`, para isolar se o problema era o enriquecimento) → sucesso técnico, mas 0 itens retornados novamente.
3. `crawlerbros/linkedin-people-search-scraper` (busca de diagnóstico por "Sidney Alberti", mesmo cookie) → erro explícito de sessão: "Could not establish LinkedIn session. Please provide fresh LinkedIn cookies." — coleta interrompida conforme regra da rotina.
