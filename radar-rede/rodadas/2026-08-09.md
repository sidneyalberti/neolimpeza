# Radar de Rede — 2026-08-09

## Execução interrompida: cookie `li_at` inválido/expirado (2ª rodada seguida com esse bloqueio)

O cookie de sessão `li_at` fornecido para esta rotina **falhou na autenticação com o LinkedIn**, assim como na rodada anterior (2026-08-08). Conforme a regra da rotina, a coleta foi interrompida imediatamente neste ponto — nenhuma conexão, post ou comentário foi coletado ou processado nesta rodada.

### Evidência do erro
- Ator `scrapeflow/linkedin-network-scraper` (conexões de 1º grau, autenticado, `max_connections: 5`, `enrichProfiles: false` como diagnóstico rápido) → sucesso técnico (exit code 0, 57.8s) mas **0 conexões** retornadas, sem erro explícito no resultado do run — o mesmo padrão ambíguo observado em 2026-08-08.
- Como esse resultado (0 itens, sem erro visível) era ambíguo, foi feito o mesmo teste de diagnóstico usado na rodada anterior: um segundo ator independente, `crawlerbros/linkedin-people-search-scraper`, com o mesmo valor de cookie, buscando "Sidney Alberti". Este ator retornou a mensagem explícita: **"Could not establish LinkedIn session. Please provide fresh LinkedIn cookies."**
- Essa mensagem confirma que a causa raiz do resultado vazio no primeiro ator é o cookie `li_at` expirado ou inválido — não um problema pontual de um único ator, e não um problema de quota (nenhum dos dois runs indicou limite de uso excedido).

### Ação necessária do Sidney
Capturar um **novo** valor de cookie `li_at` (login no linkedin.com → DevTools → Application → Cookies → `li_at`) e atualizar esta rotina com o novo valor. O valor fornecido nesta execução (diferente do usado em 2026-08-07, que funcionou) não autenticou em nenhum dos dois atores testados — mesmo comportamento da rodada de 2026-08-08. Como isso já é a 2ª rodada seguida com cookie inválido logo após a única coleta bem-sucedida do histórico (2026-08-07), vale considerar capturar o cookie momentos antes de atualizar a rotina, já que a sessão parece estar expirando rapidamente.

## Itens por categoria
- Sinal comercial explícito: 0
- Comentário/menção sem resposta: 0
- Post novo relevante: 0

## Checagem HubSpot
Não realizada — nenhuma coleta de conexões ou posts foi possível nesta rodada, então não havia nenhum perfil com sinal comercial explícito para checar.

## Limitações desta rodada
- **Cookie `li_at` inválido/expirado**: bloqueou toda a coleta (conexões de 1º grau, posts das conexões, posts e comentários do próprio Sidney). Confirmado por mensagem de erro explícita de um ator de diagnóstico (`crawlerbros/linkedin-people-search-scraper`).
- Nenhum perfil privado, rate limit ou problema de quota Apify foi identificado — o bloqueio é especificamente de autenticação de sessão do LinkedIn.

## Histórico do dia
1. `scrapeflow/linkedin-network-scraper` (conexões de 1º grau, `max_connections: 5`, `enrichProfiles: false`, diagnóstico rápido) → sucesso técnico, mas 0 itens retornados.
2. `crawlerbros/linkedin-people-search-scraper` (busca de diagnóstico por "Sidney Alberti", mesmo cookie) → erro explícito de sessão: "Could not establish LinkedIn session. Please provide fresh LinkedIn cookies." — coleta interrompida conforme regra da rotina.
