# Radar de Rede — 2026-08-10

## Execução interrompida: cookie `li_at` inválido/expirado (mais uma rodada seguida com esse bloqueio)

O cookie de sessão `li_at` fornecido para esta rotina **falhou na autenticação com o LinkedIn**. Conforme a regra da rotina, a coleta foi interrompida imediatamente neste ponto — nenhuma conexão, post ou comentário foi coletado ou processado nesta rodada.

### Evidência do erro
- Diagnóstico rápido com o ator `crawlerbros/linkedin-people-search-scraper` (busca por "Sidney Alberti", `maxResults: 3`, mesmo cookie fornecido nesta rotina) → sucesso técnico (exit code 0, ~60s) mas **0 itens** retornados, com mensagem explícita de status: **"Could not establish LinkedIn session. Please provide fresh LinkedIn cookies."**
- Isso confirma causa raiz de autenticação (cookie expirado/inválido) — não é problema de quota/créditos Apify (nenhuma mensagem de limite de uso foi retornada).
- Como o erro já veio explícito nesse diagnóstico, a coleta foi interrompida aqui, sem gastar chamadas adicionais em atores de conexões/posts.

### Ação necessária do Sidney
Capturar um **novo** valor de cookie `li_at` (login no linkedin.com → DevTools → Application → Cookies → `li_at`) e atualizar esta rotina com o novo valor. Este é o **6º dia seguido** (2026-08-05 a 2026-08-10, salvo poucas exceções) em que a rotina é bloqueada por esse mesmo motivo — a sessão parece expirar muito rápido após cada atualização de cookie. Vale considerar: (a) capturar o cookie momentos antes de atualizar a rotina, e/ou (b) verificar se a conta LinkedIn do Sidney está sendo desconectada por outro dispositivo/login, o que encerraria a sessão prematuramente.

## Itens por categoria
- Sinal comercial explícito: 0
- Comentário/menção sem resposta: 0
- Post novo relevante: 0

## Checagem HubSpot
Não realizada — nenhuma coleta de conexões ou posts foi possível nesta rodada, então não havia nenhum perfil com sinal comercial explícito para checar.

## Limitações desta rodada
- **Cookie `li_at` inválido/expirado**: bloqueou toda a coleta (conexões de 1º grau, posts das conexões, posts e comentários do próprio Sidney). Confirmado por mensagem de erro explícita do ator de diagnóstico.
- Nenhum perfil privado, rate limit ou problema de quota Apify foi identificado — o bloqueio é especificamente de autenticação de sessão do LinkedIn.

## Histórico do dia
1. `crawlerbros/linkedin-people-search-scraper` (busca de diagnóstico por "Sidney Alberti", `maxResults: 3`) → erro explícito de sessão: "Could not establish LinkedIn session. Please provide fresh LinkedIn cookies." — coleta interrompida conforme regra da rotina.
