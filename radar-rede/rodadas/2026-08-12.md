# Radar de Rede — 2026-08-12

## Execução interrompida: cookie `li_at` inválido/expirado (8ª rodada seguida com esse bloqueio)

O cookie de sessão `li_at` fornecido para esta rotina **falhou na autenticação com o LinkedIn**. Conforme a regra da rotina, a coleta foi interrompida imediatamente neste ponto — nenhuma conexão, post ou comentário foi coletado ou processado nesta rodada.

### Evidência do erro
- Diagnóstico rápido com o ator `crawlerbros/linkedin-people-search-scraper` (busca por "Sidney Alberti", `maxResults: 3`, cookie fornecido nesta rotina) → sucesso técnico (exit code 0, ~56s) mas **0 itens** retornados, com mensagem explícita de status: **"Could not establish LinkedIn session. Please provide fresh LinkedIn cookies."**
- Mesmo comportamento das rodadas de 2026-08-05 a 2026-08-11 (7 dias consecutivos), agora com um valor de cookie novamente diferente (atualizado nesta rotina) — ou seja, o novo valor fornecido também não estabeleceu sessão válida.
- Causa raiz é de autenticação (cookie expirado/inválido/sessão encerrada) — não é problema de quota/créditos Apify (nenhuma mensagem de limite de uso foi retornada).
- Como o erro já veio explícito nesse diagnóstico, a coleta foi interrompida aqui, sem gastar chamadas adicionais em atores de conexões/posts/HubSpot.

### Ação necessária do Sidney
Este é o **8º dia seguido** em que a rotina é bloqueada por esse motivo, mesmo com um cookie supostamente atualizado a cada rodada. Isso reforça que o problema não é apenas "cookie envelhecido entre rodadas diárias", e sim algo estrutural na captura/transmissão do cookie:
- (a) Confirmar que o valor de `li_at` colado na rotina foi copiado **por completo e sem espaços/quebras de linha/caracteres cortados** — cookies truncados falham exatamente do mesmo jeito que cookies expirados.
- (b) Capturar o cookie **imediatamente antes** de disparar a rotina (idealmente na mesma sessão do navegador em que a rotina será atualizada), não horas antes.
- (c) Verificar se a conta LinkedIn do Sidney está sendo desconectada por outro dispositivo/login, por 2FA, ou por política de segurança do LinkedIn (login em novo IP/dispositivo pode invalidar sessões antigas quase na hora — e isso bateria com 8 falhas seguidas mesmo trocando o valor).
- (d) Testar o cookie manualmente (colar no navegador via DevTools em uma aba anônima, com `li_at` como cookie do domínio `.linkedin.com`) logo após capturá-lo, para confirmar que a sessão está realmente ativa antes de repassá-lo à rotina.
- (e) Como alternativa, considerar rodar esta rotina manualmente uma vez, imediatamente após login fresco no LinkedIn, para isolar se o problema é o tempo entre captura e uso, ou o próprio valor/formato do cookie.

## Itens por categoria
- Sinal comercial explícito: 0
- Comentário/menção sem resposta: 0
- Post novo relevante: 0

## Checagem HubSpot
Não realizada — nenhuma coleta de conexões ou posts foi possível nesta rodada, então não havia nenhum perfil com sinal comercial explícito para checar.

## Limitações desta rodada
- **Cookie `li_at` inválido/expirado**: bloqueou toda a coleta (conexões de 1º grau, posts das conexões, posts e comentários do próprio Sidney). Confirmado por mensagem de erro explícita do ator de diagnóstico.
- Nenhum perfil privado, rate limit ou problema de quota Apify foi identificado — o bloqueio é especificamente de autenticação de sessão do LinkedIn.

## Histórico do dia
1. `crawlerbros/linkedin-people-search-scraper` (busca de diagnóstico por "Sidney Alberti", `maxResults: 3`) → erro explícito de sessão: "Could not establish LinkedIn session. Please provide fresh LinkedIn cookies." — coleta interrompida conforme regra da rotina.
